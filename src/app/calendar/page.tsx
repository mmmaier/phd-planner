import { PagePlaceholder } from "@/components/ui/page-placeholder";

export default function CalendarPage() {
  return (
    <PagePlaceholder
      title="Calendar"
      description="Month and week views with tasks, deadlines, meetings, and milestones arrive in phase 4."
    />
  );
}
