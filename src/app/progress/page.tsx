import { PagePlaceholder } from "@/components/ui/page-placeholder";

export default function ProgressPage() {
  return (
    <PagePlaceholder
      title="Progress"
      description="A log of what you've actually accomplished, week over week, arrives in phase 7."
    />
  );
}
