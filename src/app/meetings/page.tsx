import { PagePlaceholder } from "@/components/ui/page-placeholder";

export default function MeetingsPage() {
  return (
    <PagePlaceholder
      title="Supervisor meetings"
      description="Agendas, notes, decisions, and action items arrive in phase 7."
    />
  );
}
