import { PagePlaceholder } from "@/components/ui/page-placeholder";

export default function SettingsPage() {
  return (
    <PagePlaceholder
      title="Settings"
      description="Import/export, data reset, and routine configuration arrive in phase 8."
    />
  );
}
